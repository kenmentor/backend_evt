import crudRepository from "./CRUD";
import house_repository from "./house-repository";
import user_repository from "./user-repository";
import booking_repo from "./booking-repository";
import request_repo from "./request_repository";
import demand_repo from "./demand_repo";
import verification_repository from "./verification-repository";

export {
  crudRepository,
  house_repository,
  user_repository,
  booking_repo,
  request_repo,
  demand_repo,
  verification_repository,
};
